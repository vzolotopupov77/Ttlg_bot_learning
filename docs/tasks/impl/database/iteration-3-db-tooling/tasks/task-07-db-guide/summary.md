# Summary: Задача 07 — Практическая справка `docs/tech/db-guide.md`

## Статус: ✅ Done

## Что сделано

Создан файл [`docs/tech/db-guide.md`](../../../../../tech/db-guide.md).

### Структура документа

| Раздел | Содержание |
|--------|-----------|
| 1. Структура слоя данных | Дерево файлов, таблица ответственности модулей |
| 2. Миграции | Существующие make-цели; полный набор alembic-команд через `uv run`; правило «модель + ревизия в паре» |
| 3. Репозитории | Паттерн (модульные функции + flush + commit в сервисе); типовой шаблон; реальный пример из `users.py` |
| 4. Сессия в FastAPI | Пример `Annotated[AsyncSession, Depends(get_session)]`; схема цикла жизни сессии |
| 5. SQL-сниппеты | 5 запросов: пользователи, занятия ученика, просроченные ДЗ, последние сообщения, прогресс |

### Уточнения относительно плана

**Паттерн репозиториев:** в плане задачи описан шаблон на основе классов. При анализе кода установлено, что фактически используются **модульные async-функции** (не классы). Документ отражает реальную реализацию.

**Транзакционная граница:** `flush()` — в репозиториях; `commit()` — в сервисном слое (`dialogue_service.py`). Это уточнение добавлено в раздел «Сессия в FastAPI».

### make-команды в справке

| Команда | Статус |
|---------|--------|
| `make backend-db-up` | ✅ существует в Makefile |
| `make backend-db-migrate` | ✅ существует в Makefile |
| `make backend-db-shell` | ⚠️ помечена как «итерация 4, задача 08» |
| `make backend-db-reset` | ⚠️ помечена как «итерация 4, задача 08» |

## Отклонения от плана

Шаблон репозитория переработан с классового на функциональный — в соответствии с фактической реализацией.

## Проверка DoD

- ✅ Файл `docs/tech/db-guide.md` создан
- ✅ Содержит 4 основных раздела (структура, миграции, репозитории, сессия)
- ✅ Все make-цели: существующие — без оговорок; несуществующие — явно помечены «итерация 4»
- ✅ 5 SQL-сниппетов синтаксически корректны для PostgreSQL и схемы из `data-model.md`
- ✅ Нет ссылок на несуществующие файлы
