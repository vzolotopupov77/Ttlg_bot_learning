# Summary: Задача 06 — Ревью и актуализация ADR-002

## Статус: ✅ Done

## Что сделано

Обновлён [`docs/adr/adr-002-orm-migrations-tests.md`](../../../../adr/adr-002-orm-migrations-tests.md).

### Изменения в секции «Решение» (добавлены пункты 5–7)

| Пункт | Содержание |
|-------|-----------|
| 5 | UUID PK: `Uuid(as_uuid=True)` + `default=uuid.uuid4` во всех таблицах |
| 6 | StrEnum + `SQLEnum(native_enum=True, values_callable=...)` — паттерн для всех enum-полей |
| 7 | `asyncio_mode = "auto"` + `asyncio_default_fixture_loop_scope = "function"` в корневом `pyproject.toml` |

### Изменения в секции «Последствия»

- Уточнён пункт об изоляции тестов: SQLite+aiosqlite (`TTLG_ALLOW_SQLITE_TEST=1`) — явно обозначено как **временное** решение
- Ссылка на PostgreSQL harness исправлена: «задача 08» → «итерация 5, задача 13»
- Добавлена ссылка на `docs/tech/db-guide.md`
- Уточнены dev-зависимости с указанием конкретных пакетов

### Обновлена дата

`2026-03-29` → `2026-04-04`

## Отклонения от плана

Нет. Разделы «Контекст» и «Рассмотренные варианты» не тронуты — актуальны.

## Проверка DoD

- ✅ ADR содержит явные пункты: UUID PK, StrEnum+native_enum, asyncio_mode
- ✅ Ссылка на harness указывает на итерацию 5 / задачу 13
- ✅ SQLite-изоляция описана как временное решение с явным сроком замены
- ✅ Дата ADR обновлена
- ✅ «Контекст» и «Рассмотренные варианты» — не тронуты
