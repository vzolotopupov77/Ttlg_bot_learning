# Задача 03: Актуализация логической модели — итог

**Статус:** ✅ Done

---

## Что сделано

Обновлены таблицы всех 6 сущностей в [`docs/data-model.md`](../../../../../../data-model.md):

| Изменение | Было | Стало |
|-----------|------|-------|
| PK всех сущностей | `UUID / int` | `UUID` |
| `User.telegram_id` тип | `int (nullable)` | `BIGINT (nullable, UNIQUE)` |
| `User.telegram_id` constraint | не указан | `UNIQUE` |
| Timestamps | `datetime` | `TIMESTAMPTZ` |
| `User.name` длина | `string` | `VARCHAR(255)` |
| `Lesson.topic` длина | `string` | `VARCHAR(512)` |
| FK-каскады | не задокументированы | `CASCADE` / `SET NULL` для каждого FK |
| `DEFAULT now()` | не указан | зафиксирован для `created_at`, `started_at` |
| `DEFAULT 0` в Progress | не указан | зафиксирован для счётчиков |

## Решения по пробелам схемы

| ID | Пробел | Решение |
|----|--------|---------|
| G-01 | Нет `Topic` | **Отложено** — `Lesson.topic VARCHAR(512)` достаточно для MVP |
| G-02 | Нет `Material` | **Отложено** — зависит от G-01 |
| G-03 | Нет механизма переноса занятия | **Отложено** — не блокирует MVP |
| G-04 | `Progress` — пересчёт не определён | **Зафиксировано**: вручную через API; автоматический триггер — будущая итерация |
| G-05 | `Lesson.notes` — одно поле | **Подтверждено** как достаточное для MVP |

## Отклонений от плана нет
