# Задача 17: `.env.example` — полнота

**Статус:** завершена (см. [summary.md](summary.md)).

## Что меняется

- Корневой `.env.example` — сверка с `backend/src/ttlg_backend/config.py` (`Settings`) и `src/ttlg_bot/config.py`; комментарии к каждой переменной; уточнение `TTLG_ALLOW_SQLITE_TEST`.

## Файлы

- `.env.example`
- при необходимости `README.md` (ссылка на `.env.example` — проверка наличия)
