# Задача 18: синхронизация документации — summary

**Статус:** завершена.

## Сделано

- `README.md`: статусы итераций 2–3 ✅; OpenAPI — ссылка на `make openapi-export` и `docs/openapi.json`.
- `docs/plan.md`: итерации 2–3 ✅ Done.
- `docs/vision.md`: `BACKEND_TIMEOUT` в списке переменных бота.
- `docs/integrations.md`: таблица CRUD + `/health`; формулировка про `/docs` и выгрузку схемы.

## Ручная проверка

- README end-to-end на **SQLite** (ветка без `backend-db-up` / migrate): ок.

## Отклонения

- Нет.
