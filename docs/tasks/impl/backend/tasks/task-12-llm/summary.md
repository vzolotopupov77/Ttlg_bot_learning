# Задача 12: LLM-клиент и промпт — summary

**Статус:** завершена

## Сделано

- `LLMClient.complete_chat(system_prompt=..., user_message=...)` с настраиваемым таймаутом.
- Ошибки/таймауты OpenAI-compatible API → `LLMUnavailableError` (без утечки деталей клиенту — обрабатывается в роутере диалога).
- Промпт: имя ученика + последние занятия и ДЗ из БД (строки в system-роле).
- Тесты: `test_llm_client.py` + `respx`; проверка, что секрет не попадает в caplog.

## Документация

- Синхронизация подробностей с `docs/integrations.md` может быть выполнена в задаче 16 / при необходимости.
