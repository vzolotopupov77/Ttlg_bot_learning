# Summary — Задача 13

Статус: ✅ Done.

## Сделано

- `LessonDialog`: форма ученик/дата/время/тема/заметки; флаги в режиме редактирования; `AlertDialog` для удаления; заглушки действий «ДЗ»/файл/напомнить.
- Флаги: источник истины `lesson.flags` + опциональный `flagsOverride` при оптимистичном `PATCH .../flags`; сброс при смене данных (`editSyncKey`).

## Ручная приёмка

- Пройдена (2026-04-16): создание/редактирование/удаление занятия, переключение флагов — состояние Switch совпадает с карточкой в календаре после сохранения/обновления.
