# Задача 23 — Ревью кода и тема по макетам: план

## Цель

- Провести ревью `frontend/src/` по `vercel-react-best-practices` и `nextjs-app-router-patterns`; исправить критические находки.
- Перенести **цветовую палитру из макетов** в `frontend/src/app/globals.css`: только семантические переменные shadcn для `:root` и `.dark` (`--primary`, `--primary-foreground`, `--ring`, `--accent`, при необходимости `--chart-*`, sidebar).
- Не добавлять в компоненты raw-классы вида `bg-blue-500` — правило из итерации 2 / задачи 08.

## Что меняется

| Файл / область | Описание |
|----------------|----------|
| `frontend/src/app/globals.css` | Подбор OKLCH (или конвертация из макетов) для акцента и нейтральных поверхностей |
| Роуты `(app)` | `loading.tsx` / `error.tsx` по необходимости |
| Итоговый отчёт | `summary.md` задачи и итерации |

## Зависимости

- Завершены экраны итераций 3–6 (чтобы палитра проверялась на полном UI).
- Наличие макетов или экспорта цветов из Figma и т.п.

## Критерии готовности

- Сборка и линт без ошибок.
- Визуально заметен акцентный цвет по макету; light/dark читаемы.
- В summary указано, откуда взяты значения (файл макетов / таблица токенов).
