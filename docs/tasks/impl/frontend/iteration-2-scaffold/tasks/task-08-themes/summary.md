# Задача 08 — Темы: summary

## Статус

✅ Done

## Результат

- Установлен `next-themes`, `ThemeProvider` в `src/components/providers.tsx` (`attribute="class"`, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange`).
- Тема оформлена через CSS-переменные shadcn в `globals.css` (без ручных `dark:` для цветов в компонентах).
- Добавлен `src/components/theme-toggle.tsx`.
